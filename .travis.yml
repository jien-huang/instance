language: java
jdk:
  - openjdk8
dist: xenial
services:
  - docker

env:
  - TERM=dumb

script:
  - env TRAVIS=true gradle build

deploy:
  - provider: script
    script: gradle pushDockerImage
    skip_cleanup: true
    on:
      branch: master

